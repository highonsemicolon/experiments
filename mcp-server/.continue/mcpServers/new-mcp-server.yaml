name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: New MCP server
    command: npx
    args:
      - "-y"
      - "mcp-remote"
      - "http://localhost:3001/mcp"
      - "--transport"
      - "http-only"
    env: {}
